---
title: Как написать шахматную доску на larana
keywords: larana, ssr, lareq, js, javascript, ssg, блог, веб, веб-разработка, SEO
description: Пишем шахматную доску на larana. Подходы и подводные камни
---

# Как написать шахматную доску на Larana

## Что такое Larana

Larana — это прежде всего философия в разработке графических интерфейсов и только потом фреймворк. И эта философия предполагает наличие нескольких важных компонентов:

- Фреймворк. Управляет всем.
- Layout. Расчитывает положение и размеры элементов на экране.
- Larana Render Queue (lareq). Содержит в себе набор команд для отрисовки.
- Рендерер (Colorana). Принимает очередь и отрисовывает её.

Фреймворк сейчас находится в стадии глубокого переосмысления, но остальные компоненты уже можно использовать в своих проектах. Например, есть библиотека [chartography](https://github.com/laranatech/chartography), которая позволяет рисовать графики.

> Скоро тут будет ссылка на более подробный текст, но пока можно вдохновиться философией из записи доклада [LaranaJS: Настоящий SSR](https://www.youtube.com/watch?v=G1laetClEEg) на HolyJS Autumn 2024

## Как работает очередь

[Lareq](https://github.com/laranatech/lareq) представляет API для составления очереди команд. Если вы просто разрабатываете какой-то интерфейс, то об этом даже не нужно задумываться. Но если вам нужно сделать что-то на более низком уровне, то такая возможность у вас всё ещё есть — просто составьте очередь самостоятельно.

Этот низкоуровневый доступ полезен, если вы создаёте не классическое веб-приложение, а, например, какую-нибудь игру. Вот пример, где на lareq + colorana была написана [имплементация Doom](https://github.com/laranatech/doorana).

![Doorana: имплементация Doom на Larana](/assets/larana/doorana.webp)

> Смотрите запись доклада [Larana vs CSS]((https://www.youtube.com/watch?v=G-jI_JuPQNQ)) на HolyJS 2025 spring

## Рисуем шахматную доску

Но а мы начнём погружение с чего-то более простого — шахматной доски:

![Шахматная доска на компонентах Larana](/assets/larana/chessboard.webp)

Самый простой сособ сделать это — цикл внутри цикла и отрисовка каждого квадрата по отдельности. Для каждой клетки нам нужно дать три команды:
1. Указать цвет.
2. Указать фигуру.
3. Закрасить.

Вот как это выглядит в коде:

```ts
for (let i = 0; i < 8; i ++) {
	for (let j = 0; j < 8; j++) {
		// Определяем цвет для каждой клетки отдельно
		queue.command.setCtx({ fillStyle: getRectColor(i, j) })
		queue.command.rect(getRectBox(i, j))
		queue.command.fill()
	}
}
```

И вот какая из этого получается очередь:

```json
[
	{ "c": 25, "o": { "fillStyle": "white" } },
	{ "c": 15, "o": { "x": 64, "y": 64, "w": 64, "h": 64 } },
	{ "c": 9 },
	// ...
]
```

Итого получается `3 × 64 = 192` команды. Кажется, мы можем придумать что-то получше.

### Рисуем сначала все белые, потом все чёрные

Мы можем сократить количество команд, если чутка изменим алгоритм — укажем цвет, разметим все клетки этого цвета, а потом закрасим:

```ts
queue.command.setCtx({ fillStyle: LIGHT_CELL_COLOR })
for (...) {
	queue.command.rect(getRectBox(...))
}
queue.command.fill()

queue.command.setCtx({ fillStyle: DARK_CELL_COLOR })
for (...) {
	queue.command.rect(getRectBox(...))
}
queue.command.fill()
```

Чуть изменив подход мы смогли сократить количество команд почти в 3 раза: `(32 + 2) × 2 = 68`. Можем ли мы придумать что-то ещё более эффективное?

### Рисуем белый фон, а потом только чёрные

Зачем рисовать отдельно каждую клетку, если можно закрасить один большой квадрат, а потом просто рисовать поверх него?

```ts
// Рисуем фон
queue.command.setCtx({ fillStyle: LIGHT_CELL_COLOR })
queue.command.rect({ x: 0, y: 0, w: WIDTH, h: HEIGHT })
queue.command.fill()

// Рисуем всё остальное
queue.command.setCtx({ fillStyle: DARK_CELL_COLOR })
queue.command.beginPath()

for (let i = 0; i < 8; i++) {
	for (let j = 0; j < 8; j++) {
		if (isLight(i, j)) {
			continue
		}
		queue.command.rect(getRectBox(i, j))
	}
}
queue.command.fill()
```

Итого: `3 + 3 + 32 = 38`. Сокращение почти в 2 раза по сравнению предыдущим способом.

## Можно ли выжать ещё хоть что-то?

В последнем примере мало что можно оптимизировать, но с посленего релиза v.1.1.6 lareq научился вырезать дублирующиеся параметры. Вот как это влияет на каждый из этих способов рисования доски:

|--|--|--|--|--|
| **Метод** | **Длина очереди** | **Размер (КБ)** | **Сжатая длина** | **Сжатый размер (КБ)** |
| Рисуем каждую ячейку отдельно | 192 | 5 505 | 130 | 3 335 |
| Рисуем сначала белые, потом чёрные | 68 | 2 839 | 68 | 2 839 |
| Рисуем чёрные на белом фоне | 38 | 1 513 | 38 | 1 513 |

Чем меньше значения, тем лучше.

Как видно, на последних двух вариантах алгоритм проигрывает человеческому интеллекту, но это большое обновление для тех, кто не готов оптимизировать свои приложения на более низком уровне.

> За написание алгоритма спасибо [@iamshvetsov](https://github.com/iamshvetsov)

